# similar to list, cannot modify them
# cannot mutate or change them
# () this list is a tuple list
# you can only get information, not change it 
numbers = (1, 2, 3)
print(numbers[0])

# Error message if you try to add, change list
numbers = (1, 2, 3)
numbers[0] = 10
print(numbers[0])