import Converters
from Converters import kg_to_lbs

kg_to_lbs(100)

print(Converters.kg_to_lbs(70))

# we use modules to better organize our code
# instead of writing all code in one file, we break it up
# each file is called a module and should contain all of the function and classes
# Then we can import module into another module

