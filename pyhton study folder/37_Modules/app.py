# call function find_max()
# put function in different module called utils
# import into current module
# print result

from utils import find_max

numbers = [10, 3, 6, 2]
max = find_max(numbers)
print(max)